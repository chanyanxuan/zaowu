# 造物工坊 · 商业化上线清单

## 🚀 让任何人访问:两条路

### 路线 A:内网穿透(今天就能试,免费~几十元)
适合先给朋友/客户试用。用 cpolar / 花生壳 / frp 把本机 5000 端口(网页)和 3245 端口(3D 预览)
映射成公网地址,把 `.env` 的 `VIEWER_BASE` 指向 3245 的公网地址即可。
**注意:开公网前必须先把 REQUIRE_AUTH=1,否则任何人可以不登录白烧你的 token。**

### 路线 B:正式上线(域名 + 香港服务器,Docker 一键,当天可用)
1. 买域名(阿里云/腾讯云,55~85 元/年,实名);
2. 买香港轻量服务器(腾讯云/阿里云 2核2G,24~60 元/月,Ubuntu 22.04)——**免备案**;
3. 域名解析 A 记录 → 服务器 IP;
4. 上传 `zaowu-deploy.zip`(已打包好,3.4MB)→ 服务器解压;
5. 复制 `.env.example` 为 `.env`,填入:DeepSeek/Kimi 密钥、域名、短信与支付参数、
   `REQUIRE_AUTH=1`、改掉 `ADMIN_PASSWORD`;
6. 服务器装 Docker 后执行:`docker compose up -d --build`;
7. 配置 HTTPS(certbot),打开 `https://你的域名` 验收。

> 服务器买好后把 IP/SSH 给我,第 4~7 步我可以远程帮你全部做完。

---

> 我能自动完成的(代码、配置、部署脚本)都已经做好了:
> - ✅ 手机验证码注册/登录(后端 + 前端 + 腾讯云短信/通用短信接口)
> - ✅ 充值下单 → 二维码 → 支付回调验签 → 自动加积分(先抵扣欠款)
> - ✅ 按用户计费、欠费允许、修改模型同样扣费
> - ✅ 断点续跑、看门狗、管理后台、图片压缩裁剪
> - ✅ Docker 镜像 / docker-compose / nginx / 部署包(zaowu-deploy.zip)
>
> 下面剩下的事**都必须用你自己的身份证/手机/银行卡实名**,只有你能做。
> 好消息:所有需要实名的环节,**一张个体工商户执照能全部解锁**。

---

## 第 0 步:办一张个体工商户营业执照(半天,几乎免费)

这是最关键的一步,一次性解锁「企业级短信 + 支付宝当面付 + 国内备案(可选)」。

- 方法 A(推荐):当地政务网「企业开办一网通办」全程线上办,**0 元**,1~3 个工作日下证;
- 方法 B:找代办,约 100~300 元,当天出证。
- 经营范围写「软件和信息技术服务业」「互联网信息服务」即可,不需要实际经营场所。

**没有执照的替代方案**(不推荐,只能凑合):
- 短信:找支持个人的第三方短信平台(互亿无线、螺丝帽等),把它的 HTTP 接口 URL 填进
  `.env` 的 `SMS_PROVIDER=webhook` + `SMS_WEBHOOK_URL`(已支持);
- 支付:只能走「易支付/免签」类平台(见第 2 步的 B 方案),有资金与合规风险。

---

## 第 1 步:手机验证码短信(约 45 元/千条,实名 + 几分钟)

以腾讯云为例(阿里云 2025-06 起已停止个人自用资质):

1. 注册 [腾讯云](https://cloud.tencent.com) 并完成**企业实名**(用上面的执照);
2. 开通「短信 SMS」服务,创建**签名**:`造物工坊`(审核半天~1天);
3. 创建**正文模板**,内容形如:`您的验证码为:{1},5分钟内有效。请勿泄露。`
   (变量 `{1}` 即验证码;程序里已经按这个格式发送);
4. 在控制台「访问管理」创建 API 密钥,拿到:
   `SecretId`、`SecretKey`、`SmsSdkAppId`、`TemplateId`;
5. 填入 `.env`:`SMS_PROVIDER=tencent` 及上述 4 个值。

短信成本:0.045 元/条左右,新用户一般送 100~1000 条免费额度。

---

## 第 2 步:充值自动到账(当面付,签约免费 + 0.6%/笔)

**结论再说一遍:个人收款码没有回调接口,永远无法"全程自动"。**
要实现你想要的「扫码 → 到账 → 自动加积分」,正路是支付宝当面付:

1. 注册 [支付宝开放平台](https://open.alipay.com),用执照完成企业认证;
2. 创建应用,签约「**当面付**」产品(免费签约,费率 0.6%/笔);
3. 生成**应用私钥/公钥**(平台有密钥工具,一键生成),配置到应用;
4. 拿到三个值填进 `.env`:
   - `PAY_GATEWAY=alipay`
   - `ALIPAY_APP_ID`(应用 APPID)
   - `ALIPAY_PRIVATE_KEY`(你的应用私钥)
   - `ALIPAY_PUBLIC_KEY`(支付宝公钥,用于验签回调)
5. 把回调地址填到开放平台应用设置里:`https://你的域名/api/pay/alipay/notify`
   (程序已实现该接口,验签通过后自动加积分,重复回调不会重复加)。

程序里还内置了 `PAY_GATEWAY=epay`(易支付协议,含 MD5 验签回调)作为兜底:
如果你坚持用个人码,可找一家易支付/免签平台把参数填进 `.env` 即可,但**手续费高且
有跑路/封码风险**,只建议过渡期使用。本地测试用 `PAY_GATEWAY=mock`(模拟支付)。

---

## 第 3 步:买域名 + 买服务器 + 部署(半天,约 100 元/年 + 30~60 元/月)

**为什么选香港服务器:免备案**,今天买今天就能上线;国内服务器要 ICP 备案 10~20 天
(有执照也可以,后续想换随时迁)。

1. **域名**(约 55~85 元/年):阿里云/腾讯云/Namesilo 买一个 `.com`,实名后解析
   A 记录到服务器 IP;
2. **服务器**(推荐腾讯云/阿里云「轻量应用服务器」香港区,2 核 2G 起步,
   约 24~60 元/月,新用户常有折扣),系统选 **Ubuntu 22.04/24.04**;
3. SSH 登录服务器,装 Docker:`curl -fsSL https://get.docker.com | sh`;
4. 把本项目目录上传到服务器(或 git 仓库拉取),放好:
   - `Dockerfile`、`docker-compose.yml`、`nginx.conf`、`start.sh`、`requirements_server.txt`
   - 整个 `web/`、`text-to-cad/skills/cad/`(CAD 内核)、viewer 目录(3D 预览器)
   - `.env`(按 `.env.example` 填好全部密钥)
5. 把 `nginx.conf` 里的 `yourdomain.com` 换成你的域名;
6. 启动:`docker compose up -d --build`;
7. **HTTPS 证书**:容器内执行一次
   `docker compose run --rm certbot certonly --webroot -w /var/www/certbot -d 你的域名`
   (或宿主机装 certbot),拿到证书后放开 nginx.conf 里注释掉的 443 段并重启;
8. 打开 `https://你的域名` 验收:登录 → 充值 → 扫码 → 到账 → 生成模型。

**上线后必须改的配置(安全)**:
- `.env` 里 `REQUIRE_AUTH=1`(强制登录才能生成/充值);
- `PUBLIC_BASE=https://你的域名`、`VIEWER_BASE=https://你的域名/viewer`;
- `PAY_GATEWAY` 与 `SMS_PROVIDER` 从 `mock` 换成真实供应商;
- DeepSeek/Kimi 两个 API Key 放服务器环境,不要提交到 git。

---

## 费用总表(估算)

| 项目 | 一次性 | 持续 |
|---|---|---|
| 个体工商户执照 | 0~300 元 | 无 |
| 域名 .com | 55~85 元/年 | 续费同价 |
| 香港轻量服务器 2C2G | 0 | 24~60 元/月 |
| 短信验证码 | 0(送额度) | ~0.045 元/条 |
| 支付宝当面付 | 0 | 0.6%/笔 |
| HTTPS 证书 | 0 | 0(certbot 自动续) |

> 每月固定成本约 100 元以内;短信与支付按量走,用户充值先到账,模型调用费用由
> DeepSeek/Kimi 按 token 从你的余额扣(已按 500 积分/百万 token 折算进用户账单)。

---

## 已实现功能对照(三个问题)

1. **注册登录**:`/api/sms/send` 发码(60 秒限频、5 分钟有效、5 次试错)→
   `/api/auth/login` 换 30 天 token → 前端右上角登录/退出;
2. **充值自动到账**:`/api/recharge/create` 下单出二维码 → 支付宝异步回调
   `/api/pay/alipay/notify`(RSA2 验签 + 金额核对 + 幂等)自动加积分,
   先抵扣欠款;前端轮询订单状态自动刷新余额;
3. **上线**:Docker + nginx + HTTPS 一键起,香港服务器免备案当天可上线。
