# Text-to-CAD「澄清系统」二创蓝本

> 用途:把用户的自然语言需求,通过「受约束的追问」收敛成一张**无歧义的 CAD 规格卡**,
> 再喂给生成器(build123d + cadgen)产出 STEP。
>
> 本文档包含三件可直接落地的资产:
> 1. 澄清系统提示词(可直接作为 LLM 的 system prompt)
> 2. 规格卡 JSON Schema(追问的目标数据结构)
> 3. 完整题库(每问带选项、默认值、为什么问)
>
> 附带:实物尺寸参照库、完整 few-shot 示例、图片输入适配器(照片→规格卡)、与 text-to-cad 的接入说明。

---

## 目录

1. [核心原则](#一核心原则)
2. [规格卡 JSON Schema](#二规格卡-json-schema)
3. [澄清系统提示词](#三澄清系统提示词)
4. [完整题库](#四完整题库)
5. [实物尺寸参照库](#五实物尺寸参照库)
6. [Few-shot:完整对话示例](#六few-shot完整对话示例)
7. [与 text-to-cad 生成器的接入](#七与-text-to-cad-生成器的接入)
8. [输出协议与状态机](#八输出协议与状态机)
9. [图片输入适配器(照片 → 规格卡)](#九图片输入适配器照片--规格卡)

---

## 一、核心原则

1. **追问的目标是填卡,不是聊天。** 每轮追问必须往规格卡里推进至少一个字段。
2. **够细的标准:** 两个互不相干的工程师拿到这张卡,能做出**同一个零件**。凡会产生分歧的字段,要么问到,要么写进 `assumptions` 并给默认值。
3. **每轮最多 1~3 问;每问三选一:** 给选项(带推荐默认)+「你决定」+「跳过」。用户可一路点默认,30 秒走完。
4. **模糊描述必须复述确认。** 用户说「前面留个充电口」→ 翻译成几何参数并回显「我理解是:前面开一个 12mm 宽、贯穿的通槽,对吗?」。
5. **只问影响几何/制造的字段。** 不问颜色、品牌、命名等不影响建模的事(或直接默认)。
6. **收口以用户确认为准。** 规格卡 `confirmed=false` 之前,不进入生成。

---

## 二、规格卡 JSON Schema

这是追问的**唯一目标数据结构**。澄清层每填一个字段,就是在写这张卡。

```json
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CADSpecCard",
  "type": "object",
  "required": ["name", "kind", "units", "purpose", "overall", "shape", "features", "manufacturing", "assumptions", "open_questions", "confirmed"],
  "properties": {
    "name": { "type": "string", "description": "模型名,snake_case,如 phone_stand" },
    "kind": { "enum": ["part", "assembly"] },
    "units": { "enum": ["mm", "in"], "default": "mm" },
    "purpose": { "type": "string", "description": "用途与使用场景,如:横放 6.5 寸手机,底部留充电线槽" },
    "overall": {
      "type": "object",
      "required": ["bbox", "source"],
      "properties": {
        "bbox": {
          "type": "object",
          "description": "外包络尺寸,单位见 units",
          "required": ["x", "y", "z"],
          "properties": {
            "x": { "type": "number" },
            "y": { "type": "number" },
            "z": { "type": "number" }
          }
        },
        "source": {
          "enum": ["explicit", "from_reference", "inferred"],
          "description": "explicit=用户给了具体尺寸;from_reference=由参照物估算;inferred=由用途推断"
        },
        "reference_object": { "type": "string", "description": "source 为 from_reference 时的参照物,如 credit_card" }
      }
    },
    "shape": {
      "type": "object",
      "required": ["class", "base_plane", "up"],
      "properties": {
        "class": {
          "enum": ["extruded_profile", "box_block", "revolved", "shell", "custom"],
          "description": "建模主策略:拉伸截面 / 方块叠加 / 旋转体 / 抽壳 / 自定义"
        },
        "base_plane": { "enum": ["XY", "XZ", "YZ"], "default": "XY" },
        "up": { "enum": ["+Z", "+Y", "+X"], "default": "+Z" },
        "profile_note": { "type": "string", "description": "截面/主体形状的文字描述,如 L 形截面、圆形法兰" }
      }
    },
    "features": {
      "type": "array",
      "items": { "$ref": "#/definitions/feature" }
    },
    "manufacturing": {
      "type": "object",
      "required": ["method"],
      "properties": {
        "method": { "enum": ["fdm", "sla", "cnc", "sheet_metal", "injection", "unspecified"] },
        "wall_thickness": { "type": "number", "description": "抽壳/外壳壁厚,由 method 给默认" },
        "fillet_default": { "type": "number", "description": "默认外棱圆角,由 method 给默认" }
      }
    },
    "assembly": {
      "type": "object",
      "description": "仅 kind=assembly 时存在",
      "properties": {
        "parts": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["label", "role"],
            "properties": {
              "label": { "type": "string", "description": "零件标签,如 lid" },
              "role": { "type": "string", "description": "功能角色,如 上盖、底座" },
              "spec": { "$ref": "#", "description": "递归引用:零件自身的规格卡(可省略 bbox 等)" }
            }
          }
        },
        "joints": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "fixed": { "type": "string" },
              "moving": { "type": "string" },
              "type": { "enum": ["rigid", "revolute", "linear", "ball", "cylindrical", "face_to_face", "coaxial"] },
              "offset": { "type": "number" }
            }
          }
        }
      }
    },
    "assumptions": { "type": "array", "items": { "type": "string" }, "description": "未问到但已默认的假设" },
    "open_questions": { "type": "array", "items": { "type": "string" }, "description": "仍未解决的关键问题;为空即可生成" },
    "confirmed": { "type": "boolean", "default": false }
  },

  "definitions": {
    "feature": {
      "oneOf": [
        { "$ref": "#/definitions/hole" },
        { "$ref": "#/definitions/slot" },
        { "$ref": "#/definitions/fillet" },
        { "$ref": "#/definitions/chamfer" },
        { "$ref": "#/definitions/shell" },
        { "$ref": "#/definitions/boss" },
        { "$ref": "#/definitions/rib" },
        { "$ref": "#/definitions/pocket" },
        { "$ref": "#/definitions/text" },
        { "$ref": "#/definitions/pattern" }
      ]
    },
    "hole": {
      "type": "object",
      "required": ["type", "hole_type", "count", "positions", "diameter"],
      "properties": {
        "type": { "const": "hole" },
        "hole_type": { "enum": ["through", "blind", "threaded", "clearance", "countersunk", "counterbored"] },
        "count": { "type": "integer" },
        "positions": { "type": "string", "description": "位置描述:四角/中心/等距阵列/指定坐标" },
        "diameter": { "type": "number" },
        "depth": { "type": "number", "description": "盲孔深度" },
        "screw": { "type": "string", "description": "M3/M4/M5 等,clearance 用标准孔径" }
      }
    },
    "slot": {
      "type": "object",
      "required": ["type", "width", "depth"],
      "properties": {
        "type": { "const": "slot" },
        "width": { "type": "number" },
        "depth": { "type": "number" },
        "position": { "type": "string" },
        "through": { "type": "boolean" },
        "purpose": { "type": "string", "description": "如:充电线缺口" }
      }
    },
    "fillet": {
      "type": "object",
      "required": ["type", "radius"],
      "properties": {
        "type": { "const": "fillet" },
        "radius": { "type": "number" },
        "where": { "type": "string", "description": "哪些边:全外棱/指定边" }
      }
    },
    "chamfer": {
      "type": "object",
      "required": ["type", "size"],
      "properties": {
        "type": { "const": "chamfer" },
        "size": { "type": "number" },
        "where": { "type": "string" }
      }
    },
    "shell": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": { "const": "shell" },
        "thickness": { "type": "number" },
        "open_face": { "type": "string", "description": "开口面,如 top" }
      }
    },
    "boss": {
      "type": "object",
      "required": ["type", "count", "positions"],
      "properties": {
        "type": { "const": "boss" },
        "count": { "type": "integer" },
        "positions": { "type": "string" },
        "diameter": { "type": "number" },
        "height": { "type": "number" }
      }
    },
    "rib": {
      "type": "object",
      "required": ["type", "count"],
      "properties": {
        "type": { "const": "rib" },
        "count": { "type": "integer" },
        "thickness": { "type": "number" },
        "height": { "type": "number" }
      }
    },
    "pocket": {
      "type": "object",
      "required": ["type", "depth"],
      "properties": {
        "type": { "const": "pocket" },
        "depth": { "type": "number" },
        "outline": { "type": "string", "description": "凹槽轮廓描述" },
        "position": { "type": "string" }
      }
    },
    "text": {
      "type": "object",
      "required": ["type", "content"],
      "properties": {
        "type": { "const": "text" },
        "content": { "type": "string" },
        "position": { "type": "string" },
        "emboss": { "type": "boolean", "description": "true=凸起,false=凹陷" },
        "height": { "type": "number" }
      }
    },
    "pattern": {
      "type": "object",
      "required": ["type", "feature", "count"],
      "properties": {
        "type": { "const": "pattern" },
        "feature": { "type": "string", "description": "被阵列的特征" },
        "count": { "type": "integer" },
        "spacing": { "type": "number" },
        "direction": { "type": "string" }
      }
    }
  }
}
```

---

## 三、澄清系统提示词

> 直接复制为 system prompt。方括号内的占位符由你的应用运行时替换。

```
你是「CAD 需求澄清员」。你的唯一任务:通过与用户进行简短、受约束的对话,
把 ta 的自然语言需求收敛成一张无歧义的 CAD 规格卡(JSON,见 SCHEMA),为后续
的参数化建模(build123d)提供充分输入。

## 铁律
1. 你只问会改变几何或制造的问题。颜色、品牌、命名等问题直接给默认,不要问。
2. 每轮最多问 1~3 个问题。每个问题必须给:选项(第一个是推荐默认)+「你决定」+「跳过」。
3. 用户一旦选择「跳过 / 你决定」,就用题库里的默认值填充,并把该默认写进 assumptions。
4. 模糊描述必须「复述确认」:把用户的话翻译成几何参数后回显,例如
   「我理解是:前面开一个 12mm 宽、贯穿的通槽,对吗?」——直到用户确认才写入卡。
5. 只使用下方「题库」里的问题;不要自由发挥问题库之外的问题。
6. 尺寸能用参照物表达时,优先用「实物尺寸参照库」把参照物映射成毫米,并标注 source=from_reference。
7. 每回答一轮,就把已确定的字段写入规格卡,并在对话末尾用一行「[规格卡已更新]」标记。
8. 当 open_questions 为空时,停止提问,输出一次完整的「规格卡复述」(用自然语言总结整张卡),
   请用户确认或修改。用户确认后,设 confirmed=true,输出最终规格卡 JSON 并结束。

## 输出协议
- 追问阶段:只输出自然语言提问(带选项),不输出 JSON。
- 复述确认阶段:输出「自然语言复述 + 请确认/修改」。
- 收口阶段:用户确认后,输出一个 JSON 代码块,内容为最终规格卡(必须符合 SCHEMA),
  且 confirmed=true、open_questions=[]。

## 题库
<此处插入「四、完整题库」的正文>

## 实物尺寸参照库
<此处插入「五、实物尺寸参照库」的正文>

## SCHEMA
<此处插入「二、规格卡 JSON Schema」的正文>

## 示例
<此处插入「六、Few-shot」的正文>
```

---

## 四、完整题库

> 按轮次组织。`触发` = 什么时候问;`默认` = 用户跳过时的值;`映射` = 填进规格卡哪个字段。

### 第 0 轮:意图与类型

**Q0.1 类型**
- 问:「这是一个单个零件,还是由多个零件组成的装配体?」
- 选项:`① 单个零件`(默认)/ `② 装配体(两个及以上零件配合)`
- 为什么:决定 `kind`,并决定后续问题树走向(装配体多问第 5 轮)
- 映射:`kind`

**Q0.2 用途**
- 问:「它用来做什么?(一句话即可,比如"固定相机""桌面收纳")」
- 选项:`① 我来说` / `② 跳过`(默认 → 用途留空,purpose 用 shape 描述兜底)
- 为什么:`purpose` 是推断尺寸和制造意图的最强信号
- 映射:`purpose`

### 第 1 轮:尺寸锚定(最关键,最不能逼用户)

**Q1.1 尺寸来源**
- 问:「大概多大?你可以给我具体尺寸,也可以拿熟悉的东西比一比。」
- 选项:
  - `① 给具体尺寸`(→ 追问长/宽/高 或 直径)
  - `② 拿东西比`(→ 展示参照库,点选:银行卡/手机/手掌/A4/易拉罐…)
  - `③ 只给比例`(→ 追问相对关系,绝对尺寸用默认)
  - `④ 你帮我定`(默认 → 用用途 + 默认尺寸库推断)
- 为什么:尺寸是全局锚点,定错比例后面全废;参照物法体验远好于让用户报数
- 映射:`overall.bbox` + `overall.source`

**Q1.2(仅选①时)具体尺寸**
- 问:「请按 长×宽×高(mm)给我;圆柱类给我 直径×高;不记得就写『大约』。」
- 选项:`① 我来填` / `② 跳过`(→ 转用默认尺寸库)
- 映射:`overall.bbox`

### 第 2 轮:外形与朝向

**Q2.1 外形类别**
- 问:「它整体更接近哪种形状?」
- 选项(给缩略图):`① 板/块状`(默认)/ `② 圆柱/旋转体` / `③ 异形截面(如 L 形、U 形)` / `④ 外壳(中空)`
- 为什么:决定建模主策略 `shape.class`
- 映射:`shape.class`

**Q2.2 哪面朝下(放置方式)**
- 问:「它平时哪一面接触桌面/安装面?」
- 选项:`① 最大面朝下`(默认)/ `② 我指定`(描述是哪一面)/ `③ 无所谓`
- 为什么:决定原点与 `up` 轴,影响后续所有特征定位
- 映射:`shape.base_plane` + `shape.up`

### 第 3 轮:特征清单(做成可勾选,不是填空)

> 一次问「还要加下面哪些?可多选」,逐项展开追问。每项都可「跳过」。

| 特征 | 展开追问 | 默认 |
| --- | --- | --- |
| 孔 Q3.1 | 通孔/盲孔/螺纹孔/沉头?几个?在角上/中心/阵列?直径? | 4×M4 通孔,四角 |
| 圆角 Q3.2 | 哪些边?多大? | 全外棱 R3 |
| 倒角 Q3.3 | 哪些边?多大? | 全外棱 C1 |
| 槽 Q3.4 | 位置?宽?深?贯穿?用途? | 居中,宽按用途估 |
| 抽壳 Q3.5 | 壁厚?开口面? | 壁厚 2.5,顶面开口 |
| 凸台/柱 Q3.6 | 数量?位置?直径?高? | 4 个,四角 |
| 筋 Q3.7 | 数量?厚?高? | 2 条,厚 2 |
| 凹槽 Q3.8 | 轮廓?深?位置? | 按用途估 |
| 文字/logo Q3.9 | 内容?位置?凸/凹? | 凹陷,正面 |
| 阵列 Q3.10 | 对哪个特征阵列?数量?间距? | 线性阵列 |

- 为什么:特征是最容易遗漏、也最影响「够不够细」的部分;逐项确认能兜底
- 映射:`features[]`(每项对应一个 feature 对象)

### 第 4 轮:制造意图

**Q4.1 制造方式**
- 问:「打算怎么把它做出来?不知道也没关系。」
- 选项:
  - `① 3D 打印 FDM`(默认)
  - `② 3D 打印 光固化`
  - `③ CNC 铣削`
  - `④ 钣金`
  - `⑤ 注塑`
  - `⑥ 不知道/你决定`
- 为什么:自动改写默认值——FDM → 壁厚 2.5、圆角 3;CNC → 直角可保留、标准铣刀径;钣金 → 转展开图思维
- 映射:`manufacturing.method`(联动 `wall_thickness` / `fillet_default`)

### 第 5 轮:装配专属(仅 kind=assembly)

**Q5.1 零件拆分**
- 问:「它应该拆成哪几个零件?」(可让用户口述,你回显成列表)
- 选项:`① 我来描述` / `② 帮我拆`(默认 → 按用途拆,如「外壳=上盖+下壳」)
- 映射:`assembly.parts[]`

**Q5.2 配合关系**
- 问:「零件之间怎么配合?」
- 选项:`① 固定(螺丝/卡扣/贴合)`(默认)/ `② 能转动(铰链)` / `③ 能滑动` / `④ 我只是要拼在一起看`
- 映射:`assembly.joints[]`

### 第 6 轮:确认收口

**Q6.1 复述确认**
- 输出:整张规格卡的**自然语言总结**(尺寸、外形、特征、制造、假设),问:「这样对吗?要改哪里直接说。」
- 选项:`① 确认,开始生成` / `② 我要改`(→ 回到对应轮次)
- 映射:`confirmed`

---

## 五、实物尺寸参照库

> 用户点选参照物时,映射到毫米区间。`bbox` 取区间中值,`source=from_reference`。

| 参照物 | 尺寸(mm) | 典型用途锚点 |
| --- | --- | --- |
| 银行卡/身份证 | 85.6 × 54 × 0.8 | 名片、卡扣、薄片 |
| 手机(6.1~6.7 寸) | 145~160 × 68~78 × 7~9 | 支架、壳、充电座 |
| 手掌(成人) | 约 90 × 170 | 手持工具、握把 |
| A4 纸 | 210 × 297 | 文档、面板、垫板 |
| A5 纸 | 148 × 210 | 小面板 |
| 1 元硬币 | Ø25 × 1.9 | 直径锚点 |
| 易拉罐 | Ø66 × 122 | 杯套、罐类 |
| 矿泉水瓶(550ml) | Ø65 × 240 | 瓶托、收纳 |
| 马克杯 | Ø80 × 95 | 杯垫、杯架 |
| 门把手 | 约 120 × 35 | 把手 |
| 86 型开关面板 | 86 × 86 | 面板、嵌墙件 |
| 标准鼠标 | 约 115 × 65 × 38 | 手持件比例 |
| 名片盒 | 约 95 × 62 × 15 | 收纳盒 |
| 成人鞋盒 | 约 300 × 200 × 110 | 收纳箱 |

> 扩展建议:每类参照物配一张缩略图,用户点图即可,不用读表。

---

## 六、Few-shot:完整对话示例

> 直接放进提示词的「示例」段。展示一次完整的「追问 → 复述 → 收口」。

**用户**:我想做个手机支架

**助手(Q0.1)**:
这是一个单个零件,还是多个零件组成的装配体?
① 单个零件(推荐) ② 装配体

**用户**:单个

**助手(Q0.2 + Q1.1)**:
好的。它用来做什么?(一句话即可)
另外,大概多大?你可以给具体尺寸,也可以拿熟悉的东西比。
① 给具体尺寸 ② 拿东西比(推荐) ③ 只给比例 ④ 你帮我定

**用户**:横放手机,手机 6.5 寸

**助手(复述确认)**:
我理解是:横放一部 6.5 寸手机(约 160×78mm),做它的支架。对吗?
(尺寸来源:参照物 → from_reference)

**用户**:对

**助手(Q2.1)**:
整体更接近哪种形状?
① 板/块状(推荐) ② 圆柱 ③ 异形截面 ④ 外壳

**用户**:板状,带个斜坡

**助手(Q2.2 + Q3.4)**:
它哪一面朝桌面?(① 最大面朝下,推荐)
另外,前面要留充电线缺口吗?

**用户**:最大面朝下;要留充电口

**助手(复述确认)**:
我理解是:前面居中开一个 12mm 宽、贯穿的通槽给充电线。对吗?

**用户**:对

**助手(Q3.2 + Q4.1)**:
圆角怎么处理?(① 全外棱 R3,推荐)
打算怎么制造?(① FDM 3D 打印,推荐 ② 光固化 ③ CNC ④ 钣金 ⑤ 注塑 ⑥ 不知道)

**用户**:圆角 R3,3D 打印

**助手(第 6 轮复述)**:
```
我理解的规格:
- 单个零件:手机支架
- 用途:横放 6.5 寸手机(≈160×78)
- 外形:带斜坡的板状,最大面朝下
- 特征:前面居中 12mm 宽贯穿通槽(充电线);全外棱圆角 R3
- 制造:FDM(默认壁厚 2.5)
- 假设:后仰角取 15°,插槽深取 10mm(可带手机壳)
这样对吗?要改哪里直接说。
```

**用户**:确认,开始生成

**助手(收口,输出最终 JSON)**:
```json
{
  "name": "phone_stand",
  "kind": "part",
  "units": "mm",
  "purpose": "横放 6.5 寸手机(约 160×78),底部留充电线槽",
  "overall": { "bbox": { "x": 160, "y": 78, "z": 60 }, "source": "from_reference", "reference_object": "phone_6.5inch" },
  "shape": { "class": "extruded_profile", "base_plane": "XY", "up": "+Z", "profile_note": "带 15° 后仰斜坡的支撑板" },
  "features": [
    { "type": "slot", "width": 12, "depth": 20, "position": "front_center", "through": true, "purpose": "充电线缺口" },
    { "type": "fillet", "radius": 3, "where": "all_outer_edges" }
  ],
  "manufacturing": { "method": "fdm", "wall_thickness": 2.5, "fillet_default": 3 },
  "assumptions": [ "后仰角取 15°", "插槽深取 10mm(可带手机壳)" ],
  "open_questions": [],
  "confirmed": true
}
```

---

## 七、与 text-to-cad 生成器的接入

规格卡 → CAD 简报 → build123d 代码 → cadgen 生成 STEP,链路上每一层的「翻译」规则:

1. **规格卡 → CAD 简报**(纯规则,不耗 LLM token):
   - `overall.bbox` → 简报的「总尺寸」
   - `shape` → 简报的「坐标约定 / 建模策略」
   - `features[]` → 简报的「功能特征清单」
   - `manufacturing` → 简报的「制造假设」(联动默认壁厚/圆角)
   - `assumptions` → 简报的「假设」段,原样透传
   - 产出一段符合 text-to-cad `cad-brief.md` 格式的 Markdown 文本

2. **CAD 简报 → build123d 代码**(耗 LLM token,这是唯一付费环节):
   - 把简报 + build123d 建模规则作为 prompt,让 LLM 写 `gen_step()`
   - 规格卡的 `kind` 决定用单零件写法还是 `AssemblyHelper` 写法
   - 特征对象与 build123d 操作的映射参考:

| 规格卡 feature.type | build123d 操作 |
| --- | --- |
| hole / threaded / clearance | `Hole(radius)` / `CounterSinkHole` / `CounterBoreHole` |
| slot | `with BuildSketch() as s: Rectangle(w, d)` → `extrude` → 布尔减 |
| fillet | `fillet(edges, radius)` |
| chamfer | `chamfer(edges, size)` |
| shell | `p.part.shell(thickness, openings)` |
| boss | `Cylinder(r, h)` 布尔加 |
| rib | 拉伸筋板 布尔加 |
| pocket | `extrude` 布尔减 |
| text | build123d 文字/标签 |

3. **build123d 代码 → STEP**(免费,本地):
   - `python scripts/gen <name>.step.py --write`
   - 再走 `inspect validate` + `snapshot`(见部署说明)

> 提示:第 1、3 层不耗 token;第 2 层耗。省钱时可以在第 2 层做「同参数改版只改参数、不重写整段」。

---

## 八、输出协议与状态机

澄清层的状态流转:

```
[待澄清] --逐轮追问,边问边填卡--> [待复述确认] --用户确认--> [已确认(confirmed=true)] --输出最终JSON--> [交生成器]
                  ^                          |
                  |--用户要求修改(回对应轮次)--|
```

每轮追问结束后,规格卡内部更新。判断「够细」的三个硬指标:

1. `open_questions == []`
2. `overall.bbox` 的三个数都有值(来源可以是估算)
3. `features[]` 里每个特征的关键几何参数(直径/宽/深/半径/位置)都有值,缺的进了 `assumptions`

满足这三点即可确认收口,进入生成。

---

### 附:落地清单(给工程师)

- [ ] 澄清层:系统提示词 + 规格卡 schema + 题库 + 参照库 + few-shot
- [ ] 状态管理:对话中实时维护规格卡 JSON(可存 Redis / 会话内存)
- [ ] 前端:左侧追问(选项按钮 + 自然语言输入),右侧实时规格卡(可点改)
- [ ] 参照库 UI:实物缩略图点选
- [ ] 复述确认 UI:自然语言回显 + 「确认/修改」
- [ ] 接入生成器:规格卡 → CAD 简报 → LLM 写代码 → cadgen 生成 STEP
- [ ] 计费埋点:澄清层不耗 token,生成层耗(在生成层统计)

---

## 九、图片输入适配器(照片 → 规格卡)

> 本章是「澄清系统」的一个**前置输入适配器**:用户传 1~3 张照片,多模态大模型
> 看图预填规格卡,再由澄清层只追问「图片回答不了」的缺口。它不替代澄清层,
> 而是让澄清层的对话量大幅下降——图片已经替你回答了「外形」「可见特征」这类最费嘴皮子的问题。

### 9.1 定位与工作原理

```
照片(1~3 张) ──▶ 图片输入适配器 ──▶ 规格卡草稿 + open_questions ──▶ 澄清层(只补缺口) ──▶ 生成层 ──▶ STEP
                     │ 多模态 LLM              │ 图回答不了的部分          │ 复用第八节
                     │                         └──────────────────────────┘
```

核心边界(必须写进产品,否则预期会崩):

- 照片得到的是**设计意图 + 可见几何**,不是可量测的 3D 扫描。产出是**参数化 STEP**,可编辑。
- 一张照片在数学上**无法唯一确定一个三维实体**(缺比例尺、缺背面、缺厚度)。所以
  「自动出 STEP」=「看图预填 + 追问补缺口」,**不可能零追问**。
- 多视图(正面+侧面+顶面)显著提高准确度;单张只能得到「大致像」的轮廓 + 一堆假设。

### 9.2 适配器的输入 / 输出契约

**输入:**

```jsonc
{
  "images": [ { "url": "...", "view": "front|side|top|iso|unknown" } ],  // 1~3 张
  "caption": "可选的一句文字描述,与图片一起理解"                          // 可空
}
```

**输出(视觉理解层的一次调用产物):**

```jsonc
{
  "draft_card": { /* 规格卡草稿,只填模型"有把握"的字段,见 9.3 */ },
  "open_questions": [
    { "field": "overall.bbox", "reason": "无比例尺,无法确定绝对尺寸" },
    { "field": "features",     "reason": "背面不可见,不确定是否有安装孔" }
  ],
  "image_notes": {
    "visible":    ["外形为 L 形截面", "前面有两个通孔", "边缘有圆角"],
    "not_visible": ["背面", "底面", "内部结构", "厚度"],
    "confidence": "medium"
  }
}
```

`open_questions` 就是交给澄清层去问的「图片缺口」;澄清层**不从空卡开始**,而是从
`draft_card` 继续,只问 `open_questions` 里的字段。

### 9.3 规格卡扩展:`image_input` 字段(可选)

在第二章 schema 的顶层 `properties` 里新增一个可选字段,记录图片来源与可追溯性:

```jsonc
"image_input": {
  "type": "object",
  "properties": {
    "count": { "type": "integer", "description": "使用的图片数" },
    "views": { "type": "array", "items": { "enum": ["front", "side", "top", "iso"] } },
    "has_scale_reference": { "type": "boolean", "description": "画面里是否有可用的比例参照物" },
    "scale_reference": { "type": "string", "description": "如 credit_card / coin / ruler" },
    "confidence": { "enum": ["high", "medium", "low"] }
  }
}
```

这个字段的作用:让下游生成层知道「哪些尺寸是量出来的、哪些是猜的」,也方便你在
UI 上给用户标注「此尺寸来自照片估算,建议核对」。

### 9.4 看图填卡的提示词

> 直接作为视觉理解层的 system prompt。图片以多模态消息传入。

```
你是「CAD 图像理解器」。用户会给你 1~3 张某个物体的照片(可能附带一句话描述)。
你的任务是:从照片中提取"设计意图与可见几何",预填一张 CAD 规格卡草稿,
并列出照片回答不了、需要追问用户的缺口。

## 规则
1. 只填你"有把握"的字段;任何不确定的字段,宁可留空并写进 open_questions,也不要猜。
2. 尺寸:照片若无比例参照物,绝不编造绝对尺寸(mm);把"总尺寸"留空,并在
   open_questions 里标记 overall.bbox + reason="无比例尺"。
3. 可见特征(孔/槽/圆角/凸台/文字)尽量填进 draft_card.features;数量、位置要忠于照片。
4. 照片里看不见的(背面、底面、内部、厚度)一律写进 image_notes.not_visible,
   并在 open_questions 里列出需要追问的字段。
5. 外形类别(shape.class)从整体轮廓判断:板/块状→box_block 或 extruded_profile,
   圆柱→revolved,中空→shell。
6. 有比例参照物(硬币/银行卡/尺子/手)时,标注 has_scale_reference=true 并给
   scale_reference,可用实物尺寸参照库映射出估算尺寸,source=from_reference。
7. 多张图:按视角拼合理解;视角冲突时,以正面/正视图优先,并在 image_notes 里说明。
8. 输出 JSON:{"draft_card": ..., "open_questions": [...], "image_notes": {...}},
   draft_card 必须符合 SCHEMA(confirmed=false, open_questions 由本层产出)。

## SCHEMA
<插入「二、规格卡 JSON Schema」>

## 实物尺寸参照库
<插入「五、实物尺寸参照库」>
```

### 9.5 图片缺口题库(照片模式必问)

> 插在原有题库「第 0 轮」之后、原「第 1 轮尺寸锚定」之前。每问仍是「选项+默认+你决定+跳过」。

**GQ1 比例尺(最优先,必问)**
- 问:「照片里有没有能当尺子的东西?或者告诉我某个关键尺寸(比如总长)?」
- 选项:
  - `① 有参照物`(→ 让用户点选/圈出:硬币/银行卡/尺子/手/其他)
  - `② 给一个关键尺寸`(→ 输入一个数值,系统按比例推算其余)
  - `③ 都没有,你帮我估`(默认 → 用途 + 默认尺寸,写进 assumptions,source=inferred)
- 为什么:这是照片模式的**头号信息缺口**,没有比例尺一切都是比例而非尺寸
- 映射:`overall.bbox` + `overall.source` + `image_input.scale_reference`

**GQ2 看不见的面**
- 问:「背面 / 底面 / 内部是这样的吗?」
- 选项:`① 和正面一样` / `② 平整实心`(默认)/ `③ 有安装孔/螺丝柱` / `④ 我描述` / `⑤ 再传一张照片`
- 为什么:单张照片看不到的面,是「能不能装配/安装」的关键
- 映射:`features[]`(可能新增背面孔/凸台)

**GQ3 厚度 / 深度**
- 问:「它大概多厚?」
- 选项:`① 我来说` / `② 你按制造方式默认`(默认 → FDM 实心件给常规厚)
- 为什么:照片几乎给不出可靠的第三维
- 映射:`overall.bbox.z`(或圆柱高度)

**GQ4 多视图引导(可选,用于提准确度)**
- 问:「要不要再传一张侧面或顶面?能让我做得准很多。」
- 选项:`① 传侧面` / `② 传顶面` / `③ 不用了,就按现在`(默认)
- 为什么:多视图能把「估算」升级成「接近可量测」
- 映射:`image_input.views` + `confidence`

### 9.6 多视图引导策略

- **默认不强求**,但引导文案要说明收益:「再加一张侧面,比例会更准」。
- 至少三张(正/侧/顶)时可把 `confidence` 提到 `high`,并减少 GQ2/GQ3 的追问。
- 单张照片:强制走 GQ1(比例尺),否则生成结果的绝对尺寸纯属假设,UI 必须明示「尺寸为估算」。

### 9.7 图片预处理与 token 成本

**预处理(进大模型前,本地/便宜模型做,不耗主模型 token):**
1. 客户端先压缩/降采样到合适分辨率(如最长边 1024~1568px)。
2. 用便宜视觉模型或 CV 库做「主体检测 + 裁切 + 去背景」,只把主体区送给大模型。
3. 多张图按视角打标(view)再送,方便模型拼合。

**成本估算(量级,随厂商调价变化,仅供定价参考):**

| 输入 | 折算 token 量级 | 单次成本量级(前沿多模态模型) |
| --- | --- | --- |
| 1 张 1024² 照片 | 约 1k~3k token | 约 ¥0.1~¥0.5 |
| 3 张(正/侧/顶) | 约 3k~9k token | 约 ¥0.3~¥1.5 |
| 纯文字澄清(对比) | 约 3k~8k token | 约 ¥0.05~¥0.3 |

**结论:图片模式单次成本约为纯文字的 2~5 倍,主要贵在图片本身。** 省钱优先级:
① 压缩/裁切后再送;② 单张起步、不准再补视图;③ 澄清层(文字)用便宜模型,只有
「看图」和「生成代码」两步用前沿模型。

### 9.8 图片模式状态机

```
[收到照片] --视觉理解层--> [草稿卡 + open_questions]
                              │
                              ├─ open_questions 为空 ──> [待复述确认]
                              │
                              └─ 有缺口 ──> [逐条追问缺口] --补完--> [待复述确认] --确认--> [交生成器]
```

复用第八节的状态机,只是**起始点从「空卡」变成「草稿卡」**,且追问范围被
`open_questions` 收窄。生成完成后,把 STEP 的渲染图贴回原照片旁,让用户肉眼比对,
进入「迭代循环」(改 → 再生成)。

### 9.9 落地清单追加(图片模式)

- [ ] 视觉理解层:9.4 提示词 + 多模态模型接入
- [ ] 图片上传 UI:1~3 张,支持标视角(正/侧/顶),客户端压缩
- [ ] 主体检测/裁切预处理(便宜模型或 CV 库)
- [ ] 比例参照物点选 UI(复用实物尺寸参照库,支持「圈出参照物」)
- [ ] 草稿卡展示:图片 → 预填规格卡,高亮「估算字段」与「待补缺口」
- [ ] 图片缺口追问(GQ1~GQ4)接入澄清层
- [ ] 生成后「照片 vs 渲染图」并排比对视图
- [ ] 成本埋点:图片 token 单独统计(与文字澄清分开计费)
