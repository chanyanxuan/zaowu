# Text-to-CAD 最小产品:架构与交付清单

> 基于本机已跑通的完整链路(Kimi 看图 + DeepSeek 思考 + cadgen 出 STEP)整理。
> 这是从「本地原型」走向「公开网页产品」的工程蓝图。

---

## 一、已验证的核心链路(现状)

```
照片(1~3 张) ──▶ Kimi K3 看图 ──▶ 规格描述 ──▶ DeepSeek v4-pro 写 build123d(带自纠错)
                    │                              │
                    │                              └─ 报错→喂回→重试(≤4 轮)
                    └─ few-shot 参考示例
        ──▶ cadgen 生成 STEP ──▶ 校验 + 快照 ──▶ 浏览器 3D 预览 + 下载
```

**已跑通的实测数据**(遥控器照片):
- Kimi 看图:约 1000~1500 字描述,~4000 token/张
- DeepSeek 写代码:1900~2700 字符,推理模型需 `max_tokens≥32000`
- 自纠错:平均 1~2 轮后成功
- 产出:STEP(87~187 面)+ 快照 PNG + GLB 预览

## 二、模型路由策略(核心成本杠杆)

| 环节 | 模型 | 选型理由 | 何时换 |
| --- | --- | --- | --- |
| 看图 | Kimi K3(moonshot) | 多模态,国内可访问,性价比高 | 可换千问 VL / GPT-4o |
| 写代码(简单件) | deepseek-v4-flash | 快、便宜 | — |
| 写代码(复杂/要细节) | deepseek-v4-pro | 会思考,代码质量高 | — |
| 自纠错 | 同上(flash 优先) | 修小错不必用 pro | — |

**自动路由规则(省钱关键):**
1. 简单零件(单实体、无装配)→ flash;复杂/装配/要细节 → pro。
2. 一次生成失败 → 先用 flash 修 2 轮,仍失败 → 升级 pro 修 1 轮。
3. 看图(贵)只在首次;追问/改参/重生成不再重复看图。

## 三、技术架构(公开网页版)

```
┌─ 前端 Web(React/Vue)────────────────────────────┐
│  上传照片(1~3张) · 追问对话 · 3D预览 · 下载STEP   │
│  3D 预览复用 cad-viewer(three.js,已打包 dist)    │
└──────────────┬───────────────────────────────────┘
               │ HTTPS
┌──────────────▼───────────────────────────────────┐
│ API 网关 + 后端(FastAPI/Node)                    │
│  用户认证 · 订阅计费 · 配额 · 内容审核 · 任务编排   │
└──────┬──────────────────────┬───────────────────┘
       │                      │
┌──────▼─────┐        ┌───────▼──────────────────┐
│ 模型服务层  │        │  任务队列(Redis/Celery)  │
│ Kimi /     │        └───────┬──────────────────┘
│ DeepSeek   │                │
│ (OpenAI    │        ┌───────▼──────────────────┐
│  兼容)     │        │  沙箱 Worker(Docker/gVisor)│
└────────────┘        │  cadgen+build123d        │
                      │  限内存/限时/断网         │
                      └───────┬──────────────────┘
                              │
                      ┌───────▼──────────────────┐
                      │ 对象存储(S3/OSS)          │
                      │  STEP/GLB/快照            │
                      └──────────────────────────┘
```

## 四、从本会话提炼的关键技术决策(直接照抄)

1. **few-shot 是成败关键**:给模型一段能跑通的 build123d 参考代码,成功率从「3 轮全废」跃升到「1~2 轮成功」。生产环境维护一个「参考代码库」(零件/装配体各 1~2 个样板)。
2. **推理模型必须给足 max_tokens**:deepseek-v4-pro 会先思考,`max_tokens=8000` 时思考未结束就被截断(只返回思考、无代码)。设 `≥32000`。
3. **推理模型响应结构**:代码在 `message.content`,思考在 `message.reasoning_content`;只取 `content`,别把思考当代码。
4. **自纠错闭环**:捕获 cadgen 报错 → 喂回模型 → 重试,上限 3~4 轮。报错前先用 `compile()` 做语法预检,拿精确行号。
5. **图片预处理**:进模型前压缩到最长边 ≤1280px、去透明合成白底,省 token。
6. **本机环境坑**(部署时已修):
   - build123d 扫字体遇损坏文件 `mstmc.ttf` 崩溃 → 给 `build123d/text.py` 加 try/except 跳过。
   - 快照渲染默认要 playwright chromium(192MB)→ 改用系统 Edge(`channel="msedge"`)。
   - cadgen 工具传路径要用 POSIX `/`,别用 `\`。

## 五、成本结构(量级,供定价)

| 项 | 单次量级 |
| --- | --- |
| Kimi 看图(1 张) | ~4000 token ≈ ¥0.1~0.3 |
| DeepSeek flash 写代码 | ~3000 token ≈ 几分钱 |
| DeepSeek pro 写代码(含思考) | ~10000+ token ≈ 明显更贵 |
| 自纠错(平均 +1 轮) | 加 1 倍代码成本 |
| 合计(单零件,一次成功) | 约 ¥0.3~1.5 |

**定价建议**:订阅制 + 月度生成次数配额(如 ¥19/月含 50~100 次),超量购次数包。单用户 token 成本上限约 ¥10~30,毛利健康。

## 六、交付清单(分阶段)

### 阶段 0:本地原型 ✅(已完成)
- [x] 照片→看图→写代码→自纠错→STEP 一键脚本(`text2cad_proto.py`)
- [x] 多图输入(1~3 张)
- [x] 3D 预览 + 快照 + 下载
- [x] flash/pro 模型可切换(`--code-model`)

### 阶段 1:单机 Web MVP(下一目标)
- [ ] FastAPI 后端 + 前端(上传 + 进度条 + 预览 + 下载)
- [ ] 任务队列(异步生成,不阻塞请求)
- [ ] 沙箱 Worker(Docker,限制内存/时间/网络)
- [ ] 交互式追问(接《澄清系统蓝本》题库,不只 `--note` 一句话)
- [ ] 模型路由(flash/pro 自动选)

### 阶段 2:公开产品
- [ ] 用户注册 + 登录(OAuth / 手机号)
- [ ] 订阅计费(国内:支付宝/微信;海外:Stripe)
- [ ] 月度配额 + 用量统计
- [ ] 内容审核(图片合规)
- [ ] 服务条款(版权:禁止 1:1 逆向受保护外观)

### 阶段 3:质量与规模化
- [ ] 自纠错升级(多轮 + 策略路由)
- [ ] 参考代码库扩充(更多零件/装配样板)
- [ ] 生成缓存(同规格卡命中缓存,不再重复调模型)
- [ ] 监控 + 成本告警

## 七、安全与合规红线

1. **沙箱是底线**:执行 AI 生成的 Python,必须隔离(Docker 默认禁止,或用 gVisor/Firecracker),限内存(如 512MB)、限时(如 60s)、断网。
2. **API Key 不进前端**:模型调用全部走后端,Key 只存服务端。
3. **版权**:条款明确禁止上传受保护的产品外观做 1:1 逆向;定位为「原创设计辅助」。
4. **内容审核**:上传图片先过审核 API。

## 八、本机资产索引

| 文件 | 用途 |
| --- | --- |
| `text2cad_proto.py` | 一键原型(看图→写代码→STEP,多图+自纠错) |
| `vision_kimi.py` | 单独「Kimi 看图」脚本 |
| `澄清系统-二创蓝本.md` | 追问/规格卡/题库/图片适配器规格 |
| `部署说明.md` | 本地 text-to-cad 环境部署记录 |
| `text-to-cad/` | cadgen + build123d 引擎 + 技能库 |
| `proto_out/` | 各版本生成产物(STEP/快照) |
