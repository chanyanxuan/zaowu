# text-to-cad 部署说明

项目:earthtojake/text-to-cad(⭐13.4k,MIT 协议)
GitHub:https://github.com/earthtojake/text-to-cad
文档:https://www.texttocad.dev

## 一、已安装内容

| 组件 | 版本 / 位置 |
| --- | --- |
| Git | 2.55.0 — `C:\Program Files\Git\cmd\git.exe` |
| Python | 3.12.10 — `C:\Users\Administrator\AppData\Local\Programs\Python\Python312\python.exe` |
| 仓库 | `C:\Users\Administrator\Desktop\text2cad\text-to-cad`(main 生产分支,浅克隆) |
| 虚拟环境 | `C:\Users\Administrator\Desktop\text2cad\text-to-cad\.venv` |
| 技能库 | `C:\Users\Administrator\.agents\skills\`(11 个技能,已 symlink 到各 agent) |

Python 依赖(装在 `.venv` 内):
- `cadgen 0.4.9`(text-to-cad 的 STEP 生成运行时)
- `build123d 0.11.1`(参数化建模库,基于 OpenCascade)
- `cadquery-ocp 7.9.3.1.1` / `cadquery-ocp-novtk`
- `playwright 1.62.0`(快照渲染,已改用系统 Edge,无需下载 chromium)
- 以及 `ezdxf`、`shapely`、`numpy`、`scipy`、`vtk`、`matplotlib` 等

已安装的 11 个技能:bambu-labs、cad、cad-viewer、dxf、gcode、implicit-cad、sdf、sendcutsend、srdf、step-parts、urdf。

## 二、如何生成 CAD 模型

在任意工作目录放一个 `<名称>.step.py` 生成器,然后运行:

```powershell
# 虚拟环境 Python + 技能里的 gen 脚本
$py = "C:\Users\Administrator\Desktop\text2cad\text-to-cad\.venv\Scripts\python.exe"
$gen = "C:\Users\Administrator\Desktop\text2cad\text-to-cad\skills\cad\scripts\gen"
Set-Location <你的工作目录>
& $py $gen <名称>.step.py --write     # 生成 .step 文件
& $py $gen <名称>.step.py              # 只生成 GLB/拓扑工件,不写 .step
```

生成器示例(见 `C:\Users\Administrator\Desktop\text2cad\demo\mount_plate.step.py`):

```python
from build123d import *

def gen_step():
    with BuildPart() as plate:
        Box(60, 40, 6)
        with Locations((24, 14), (-24, 14), (24, -14), (-24, -14)):
            Hole(2.5)
    return plate.part
```

更多用法:用 `& $py $gen --help` 查看;其他工具在
`skills\cad\scripts\` 下:`export`(STL/3MF/GLB)、`inspect`(校验/测量)、`snapshot`(截图)。

## 三、已做的修复(重要)

build123d 在 Windows 上导入时会扫描系统字体,而系统里有一个损坏文件
`C:\Windows\Fonts\mstmc.ttf`,会导致 `from build123d import *` 直接崩溃。

处理方式:给 `.venv\Lib\site-packages\build123d\text.py` 的 `register_font`
加了容错(跳过无法解析的字体文件),未删除系统文件。

⚠️ 注意:如果以后用 pip 重新安装/升级 build123d,该补丁会丢失,需要重新加上。
(也可以选择直接重命名 `C:\Windows\Fonts\mstmc.ttf`,但那是系统目录,更推荐补丁方式。)

### 快照渲染改用系统 Edge

`scripts/snapshot` 默认要求 playwright 自带的 chromium(约 192MB,下载慢)。
已改为使用 Windows 自带的 Microsoft Edge(在 `cadgen/snapshot_core.py` 的
`chromium.launch(...)` 里加了 `channel="msedge"`),因此无需下载 chromium。

已修改两处:
- `C:\Users\Administrator\Desktop\text2cad\text-to-cad\skills\cad\scripts\packages\cadgen\src\cadgen\snapshot_core.py`
- `C:\Users\Administrator\.agents\skills\cad\scripts\packages\cadgen\src\cadgen\snapshot_core.py`

## 四、验证结果

冒烟测试 `demo\mount_plate.step.py` 已成功:
- 生成 `demo\mount_plate.step`(35 KB)
- 生成 GLB 拓扑工件 `demo\__cadgen__\models\mount_plate.step.py\`
- `scripts/inspect` 校验 `ok: true`,无错误
- 几何:60×40×6 的板,4 个 φ5 通孔,10 个面(6 个 box 面 + 4 个孔壁)
- `scripts/snapshot` 快照渲染成功(用系统 Edge),输出 1600×1200 PNG

## 五、与 AI 助手配合使用

text-to-cad 本质是「agent 技能库」,需配合 Claude Code / Codex / Cursor 等
agent 使用。技能已装到全局 `~\.agents\skills\`,大多数 agent 会自动识别。

- 让 agent 以某个工作目录运行,把 CAD 需求用自然语言告诉它,它会调用 `cad` 技能
  写 build123d 代码并生成 STEP。
- 生成后可用 `cad-viewer` 技能在浏览器里查看模型。
