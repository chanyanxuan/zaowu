# 造物工坊:自然语言驱动的专业三维建模平台

**让每一个有建模需求的人,不必掌握复杂的三维设计软件,也能获得标准、可编辑、可直接投入工程流程的三维模型。**

造物工坊是一款基于大语言模型与程序化建模技术的在线三维建模平台。用户以自然语言描述所需零件,系统即可完成需求理解、外观调研、结构确认、自动建模与质量纠错的全流程,最终输出工业标准的 CAD 文件。

## 产品定位

在传统的产品开发流程中,三维建模是一项门槛较高的专业工作:结构设计人员需要经过长期学习,才能熟练使用专业 CAD 软件;而大量结构简单、需求明确的零件——支架、外壳、面板、固定座、连接件等——并不需要如此高昂的学习成本。

造物工坊正是面向这一需求而生:将建模的门槛从"掌握软件"降低为"描述清楚需求",让工程师之外的更多角色,也能快速获得可用的三维模型。

## 核心能力

### 一、自然语言建模

用户只需以中文描述零件,系统自动完成建模。支持文字描述与图片上传两种输入方式,亦可两者结合,进一步明确需求。

### 二、外观造型调研

在建模开始前,系统会基于内置知识,对目标物体进行外观造型分析,生成包含整体轮廓、部件构成、比例关系与特征细节的量化说明书,并以此作为建模依据,确保生成结果贴合用户预期。

### 三、智能需求确认

对于描述中未明确、但对建模结果有实质影响的信息,系统会以结构化问题清单的形式向用户确认,通常为 5 至 12 项,每项均提供基于常识的推荐答案。用户可逐项调整,也可一键采用全部推荐值,兼顾精细与高效。

### 四、自动纠错与质量保障

建模过程中,系统会自动执行、校验生成结果;出现错误时,自动分析原因并重新生成,多次迭代直至成功。用户无需介入技术细节。

### 五、参数化微调

生成完成后,模型的关键尺寸——长度、宽度、壁厚、圆角、孔径等——均以参数形式呈现。用户通过滑杆或数值输入即可调整尺寸,系统将按新参数快速重新生成。此过程为本地计算,几乎不产生额外费用。

### 六、三维预览与交互

生成结果以交互式三维模型呈现,支持旋转、缩放、面与边的点选查看、尺寸测量、零件引用等操作,界面为全中文。

### 七、模型管理与修改

所有历史生成记录均可随时回看与重新下载。用户可在三维视图中点选零件,以自然语言描述修改需求,系统仅对指定部分进行调整。

## 使用建议

**描述越详细,生成结果越精确。** 建议用户在描述中尽可能完整地提供以下信息:

- **整体外形**:形状、轮廓与大致比例;
- **关键尺寸**:长、宽、高、厚度、孔径、孔距、中心距等具体数值,不确定时可标注"大约";
- **结构特征**:孔、槽、筋、凸台、圆角、倒角等的数量与位置;
- **用途与工艺**:使用场景及预期加工方式,这将影响壁厚、圆角等默认参数;
- **装配关系**:多零件需求时,说明各零件之间的配合方式。

## 输出格式与软件兼容性

造物工坊输出的文件可直接进入专业工程流程:

- **STEP 格式**:工业通用的 CAD 数据交换标准。生成模型可在 SolidWorks、Fusion 360、FreeCAD、CATIA、NX、Creo、Onshape、中望 3D、浩辰 CAD 等主流三维设计软件中直接打开,并继续编辑、装配或出图;
- **STL 格式**:通用网格格式,兼容各类切片与三维处理软件;
- 同时保留模型的参数化源文件,供有需要的用户进行二次开发。

## 内测说明

造物工坊目前处于**内测阶段**,采用邀请码方式开放使用:

- 有意体验的用户可通过**私信获取邀请码**;
- 每个邀请码附赠 50 积分,可用于生成多个常规零件;
- 内测期间暂不开放充值,积分用尽后可再次申请邀请码。

采用邀请码模式,是为了在可控的范围内收集真实反馈,持续优化产品体验。

**后续将开放充值功能。** 每次生成背后,均涉及大语言模型的推理计算与程序化建模的运算成本,因此正式运营后将按实际用量计费。以一个常规零件为例,生成费用约为几角钱,复杂装配体相应更高。内测阶段用户无需承担上述费用。

## 关于产品边界

我们如实说明当前产品的定位与边界:

1. 造物工坊面向**零件级建模**,不替代工业级设计软件。目前不提供公差标注、工程图与强度校核,量产用途的模型建议由专业工程师复核;
2. 产品在**规则几何**(板、壳、孔、筋、凸台等)方面表现稳定;高复杂度曲面造型仍有提升空间;
3. 单个模型生成通常需要数分钟,复杂装配体时间更长,生成期间可进行其他工作;
4. 复杂需求偶有生成失败的情况,系统会提供原因说明与一键重试。

## 写在最后

从"描述一个零件"到"获得一份工程文件",我们希望造物工坊能让三维建模这件事,变得足够简单,足够直接。

欢迎访问 **zaowugongfang.com** 了解产品详情;如需内测邀请码,请私信联系。

内测期间,您的每一条反馈,都将直接帮助我们塑造产品的下一个版本。
