# 造物工坊 · 产品形态扩展方案

> 目标:把"AI 文本/照片 → 3D 模型"的核心能力,从单一网站扩展成多种产品形态。
> 本文逐一分析各形态的定位、方案、成本、收入与风险,供决策。

---

## 一、核心资产盘点(可以复用的东西)

**已实现的管线(text2cad_proto.py):**

| 能力 | 状态 | 说明 |
|---|---|---|
| 多模态输入 | ✅ | 文字 / 1~3 张照片(Kimi 视觉) |
| 需求精炼 | ✅ | 工程规格化(refine) |
| 外观调研 | ✅ | DeepSeek 内部知识生成造型规格(不联网) |
| 澄清问答 | ✅ | 5~12 个问题,均带推荐默认,一键全选 |
| 代码生成 | ✅ | build123d 代码,最多 6 次自纠错 |
| STEP/STL 输出 | ✅ | 标准 STEP + STL + 快照 PNG |
| 参数提取 | ✅ | 顶层常量 → ≤20 个中文标签滑杆 |
| 参数重建 | ✅ | 替换常量重建,**零 LLM 调用,几乎零成本** |
| 自然语言修改 | ✅ | 一句话改模型(LLM 调用) |
| 计费 | ✅ | 积分制,允许欠费,补款优先还欠款 |
| 账号 | ✅ | 邀请码 / 手机号 / 管理后台 |
| 运维 | ✅ | 全局队列(并发2)、心跳看门狗、每日备份 |

**成本结构(定价基准):**
- 1 元 = 10 积分;500 积分/百万 token ⇒ **1 元 = 100 万 token 的算力**
- 简单件实际消耗 3~10 积分(0.3~1 元),复杂装配体更高
- 参数微调 ≈ 0 成本 → 这是所有形态共用的**杀手锏功能**

**技术栈:** Python/Flask + build123d(OCP 内核,FreeCAD 同源)+ DeepSeek + CAD Viewer(WebGL,已汉化)。

---

## 二、六个形态逐一分析

### 形态 A:MCP Server + CLI(⭐ 最推荐,投入产出比最高)

- **定位:** 给 AI Agent 和工程师的"造物接口"
- **目标用户:** Claude Desktop / Cursor / 各种 Agent 用户;写脚本的机械工程师、创客
- **产品形态:**
  - MCP Server:`generate_cad(描述) → {step路径, stl路径, 预览链接, 参数表}`,`modify_cad(模型, 描述)`,`reparam_cad(模型, 参数)`
  - CLI:`pip install zaowu` → `zaowu "做个M3×20的螺栓" --out bolt.step --preview`
- **复用:** 100%(直接 import text2cad_proto;预览链接复用线上 viewer)
- **新增开发(约 5~8 人天):**
  - 把 Flask 依赖剥成"纯函数核心"(无 web 依赖)
  - MCP 协议适配(stdio)+ CLI 入口(argparse)
  - API Key 体系(users.py 加 key 字段)+ 用量计费
  - PyPI 发布 + 文档页
- **成本:** 与网站共享同一后端;2C4G 服务器在并发 >2 时需扩容(或做成队列型响应)
- **收入模型:** 按量付费(充值积分换 API Key);或 9.9 美元/月 的 Maker 订阅
- **风险:** MCP 生态变化快;需持续跟进协议版本(目前 MCP 已较稳定)
- **为什么先做:** 同一套 Python 核心、上线最快、直接吃 AI Agent 流量红利

---

### 形态 B:FreeCAD 插件(⭐ 次推荐,天然契合)

- **定位:** 把 AI 建模带进免费 CAD 软件,生成**可编辑的参数化文档**
- **目标用户:** FreeCAD 社区(全球数百万爱好者/学生/小团队)
- **产品形态:** FreeCAD Addon(工作台"造物工坊 AI"):一个输入框 + 预览面板,生成结果直接落在 FreeCAD 文档树里(草图/特征可改)
- **复用:** 管线 100%;build123d 与 FreeCAD 同 OCP 内核,兼容性天然好
- **新增开发(约 10~15 人天):**
  - FreeCAD Python API 封装(命令按钮、任务面板)
  - STEP → FreeCAD 文档导入(现成能力)
  - 内置积分/API Key 登录,复用线上计费接口
  - 通过 FreeCAD Addon Manager 分发(免商店审核)
- **成本:** 几乎无服务器增量(核心调用在用户端,模型推理仍走 API)
- **收入模型:** 免费额度 + 订阅解锁(无限生成/装配体);或企业版(本地化部署)
- **风险:** 用户付费意愿弱于工程师;B 端转化要靠口碑
- **为什么值得:** FreeCAD 是唯一能低成本覆盖"CAD 软件内"场景的入口,为将来 SolidWorks/Fusion 版验证需求

---

### 形态 C:开放 API / B2B SDK(真正的钱,但要商务)

- **定位:** 网站本身变成"演示厅",付费点变成 API
- **目标用户:** 3D 打印平台、定制电商(手机壳/铭牌/工装)、设计外包公司、SaaS 产品
- **产品形态:** REST API(`POST /v1/generate`、`POST /v1/modify`、`POST /v1/reparam`)+ 多语言 SDK(Python/JS)
- **复用:** 90%(接口已存在,补鉴权/限流/文档/计量)
- **新增开发(约 7~10 人天):** API Key 管理、速率限制、异步任务回调(webhook)、OpenAPI 文档、计费报表
- **成本:** 服务器按调用量线性扩容;需客服与 SLA 承诺
- **收入模型:** 按量(每件 X 元)+ 月保底套餐;**毛利率高**,是唯一能把积分制升级为订阅/按量付费的形态
- **风险:** 卖 API 需要销售能力;竞品(国外已有几家 text-to-CAD API)要差异化——我们的差异化就是**参数微调 + 中文澄清流程 + 装配体能力**
- **节奏建议:** 有企业来问再开,或 MCP 版跑通后顺带开

---

### 形态 D:打印机闭环("一句话 → 已打印",最性感)

- **定位:** 消费级完整闭环:文字 → STL → 云切片 → 送到 Bambu 打印机
- **目标用户:** 3D 打印玩家、创客家庭
- **产品形态:** 网站加"直连打印"按钮:生成后一键切片并推送到用户的 Bambu 打印机(LAN/云)
- **复用:** STL 输出已有;参考 bambu-labs 的 FTPS/MQTT 链路
- **新增开发(约 15~20 人天 + 硬件测试):** Bambu 账号授权、云切片调用、打印机绑定、任务状态页;硬件采购(1~2 台机器实测)
- **成本:** 云切片按次收费(Bambu 云有成本);硬件测试投入
- **收入模型:** 按打印件抽成 / 订阅("无限打印通道");或与耗材电商分成
- **风险:** 依赖 Bambu 平台策略;硬件链路测试繁琐
- **节奏建议:** 作为 MCP/API 之后的"爆点功能",选对时机投放

---

### 形态 E:微信小程序(国内付费闭环最强)

- **定位:** 国内移动端入口:拍照/一句话 → 模型 → 微信支付买积分
- **目标用户:** 国内创客、学生、手办定制人群
- **产品形态:** 微信小程序(核心流程裁到:拍照/描述 → 预览 → 下载/打印下单)
- **复用:** 后端 90%;前端需重写(小程序框架)
- **新增开发(约 20~30 人天):** 小程序前端、微信支付对接、登录体系、备案/认证流程
- **成本:** 服务器需国内节点(当前新加坡节点访问不稳定);备案周期 2~4 周
- **收入模型:** 微信支付直接收钱(解决现在"内测不发充值"的最大缺口)
- **风险:** 审核与备案时间不确定;获客需投放
- **节奏建议:** 有国内流量打法(比如 B 站/小红书内容)再启动

---

### 形态 F:SolidWorks / Fusion 360 插件(天花板最高,成本最高)

- **定位:** 专业 CAD 内的一键 AI 建模
- **目标用户:** 企业工程师(付费能力最强的人群)
- **产品形态:** Fusion 360 Add-in(Python API,过 Autodesk App Store 审核)或 SolidWorks Add-in(.NET/COM)
- **复用:** 管线 100%(Fusion 的 Python 环境可调 REST API)
- **新增开发(约 30~60 人天):** 商店审核、安装器、各版本兼容、企业授权体系
- **收入模型:** 企业订阅(99~299 美元/席位/年)
- **风险:** 个人开发者周期长、审核严格;建议 FreeCAD 版验证后做
- **节奏建议:** 放最后

---

## 三、横向对比

| 形态 | 开发成本 | 上线速度 | 收入潜力 | 风险 | 建议顺序 |
|---|---|---|---|---|---|
| A MCP + CLI | ★☆☆☆☆ | 1 周 | ★★★☆☆ | 低 | **1** |
| B FreeCAD 插件 | ★★☆☆☆ | 2~3 周 | ★★☆☆☆ | 低 | **2** |
| C 开放 API | ★★☆☆☆ | 2 周 | ★★★★☆ | 中(销售) | 3(视需求) |
| D 打印闭环 | ★★★☆☆ | 3~4 周 | ★★★☆☆ | 中(依赖平台) | 4(爆点) |
| E 微信小程序 | ★★★★☆ | 4~6 周 | ★★★★☆ | 中(审核/备案) | 视国内策略 |
| F CAD 插件 | ★★★★★ | 2~3 月 | ★★★★★ | 高 | 最后 |

---

## 四、推荐路线图(90 天)

```
第 1~2 周  A:MCP Server + CLI
           - 剥离纯函数核心(web 依赖去掉)
           - MCP stdio 适配 + CLI
           - API Key 体系 + 用量计费
           - PyPI 发布 + 一页文档站

第 3~5 周  B:FreeCAD Addon
           - 工作台 UI(输入框/预览/登录)
           - STEP 落文档树
           - Addon Manager 分发

第 6~8 周  C:开放 API(如果 MCP 验证有需求)
           - webhook 异步回调 + OpenAPI 文档 + 限流

第 9~13 周 D/E 按当时反馈二选一:
           - D 打印闭环(买 1 台 Bambu 实测)
           - E 小程序(若决定做国内)
```

**每个阶段都不动现有网站**,只是共享同一后端与计费体系。

---

## 五、关键决策点与风险提示

1. **开源策略:** 底层来自 MIT 协议(earthtojake/text-to-cad),我们的增量(外观调研、参数提取、修改流程、中文体验)建议**核心闭源、接口开放**:MCP/CLI 免费额度引流,超额付费。
2. **定价:** MCP/CLI 按量(沿用积分),API 按量 + 月保底,FreeCAD 用订阅。统一计费后端,不要三套账。
3. **服务器:** 当前 2C4G 只够网站 + 2 并发;开 API 后需换 4C8G 或拆"生成 worker"(队列化),这一笔要在定价里摊进去(约 60~150 元/月/档)。
4. **免责声明:** 所有形态都保留"零件级、非工业级"的诚实定位,避免专业用户误用导致纠纷。
5. **竞品差异:** 海外 text-to-CAD API 没有"中文澄清 + 参数微调 + 装配体"组合——文档和落地页要主打这三点。
6. **合规:** API/订阅收款(Stripe 或支付宝企业),需注册主体;个人阶段可先用 epay/易支付小规模过渡。
